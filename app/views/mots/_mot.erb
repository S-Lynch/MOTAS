<tr>
  <td class="mots">
    <span class ="mot_date"> <h1> Your M.O.T has been booked for the: </h1> <h1><%= mot.mot_date %> </h1>
    </span>
  </td>
  <% user = mot.user rescue User.find(mot.user_id) %>
  <% if current_user?(user) %>

  <td>
    <%= link_to "Delete this M.O.T", mot, :method => :delete, :confirm => "Are you sure?", :title => mot.mot_date %>
  </td>
  <% end %>
</tr>
